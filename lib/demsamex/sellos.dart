import 'package:flutter/material.dart';

class Sellos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sellos'),
      ),
      body: Center(
        child: Text('Esta es la pagina de sellos'),
      ),
    );
  }
}
