import 'package:flutter/material.dart';

class Motores extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Motores'),
      ),
      body: Center(
        child: Text('Esta es la página de motores!'),
      ),
    );
  }
}
