import 'package:flutter/material.dart';

class Bombas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bombas'),
      ),
      body: Center(
        child: Text('Esta es la pagina de bombas'),
      ),
    );
  }
}
