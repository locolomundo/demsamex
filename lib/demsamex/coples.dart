import 'package:flutter/material.dart';

class Coples extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Coples'),
      ),
      body: Center(
        child: Text('Esta es la pagina de coples'),
      ),
    );
  }
}
